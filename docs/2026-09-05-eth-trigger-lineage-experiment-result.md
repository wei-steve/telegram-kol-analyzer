# ETH 最小数量真实触发实验结果

## 结论

用户亲自执行v2命令后，两侧父触发单提交成功，多侧实际触发并成交，空侧未触发且观察结束时按本次回执ID撤销。**本次样本没有证明 ordId、clOrdId 或 tag 可以直接串起父触发单、普通执行子单与自动止损。** 普通订单详情clOrdId等于子单自身ordId，没有继承请求中的自定义clOrdId；列表和fills同字段为空。止损记录未返回明确父单/仓位引用。

这是一组多空请求、其中一个方向触发的样本，不是所有订单类型/方向的普遍契约证明。止损尚未触发，空侧也没有触发，相关后续链条仍未验证。

## 范围与证据

- 用户运行目录：`/var/lib/telegram-kol-cutover-evidence/eth-minimum-test-20260905-v2/live-b7a7234d4ada`。
- 请求manifest的独立标识：`4305ffecaf2a`。它与运行目录标识不同，使用实际请求/回执文件关联，不把目录名当clOrdId。
- 实验窗口：2026-09-05T15:14:01.469Z至15:19:11.501Z，共18个采样帧；状态observation_ended_manual_review_required。
- 当前状态刷新：2026-09-05T15:21:42Z起，目录`refresh-20260905T152142Z`，完整原文为raw.jsonl，概要为summary.json。
- 原始提交记录：submit-long-request.json、submit-long-response.json、submit-short-request.json、submit-short-response.json。
- 原始GET：raw.jsonl；完整字段归纳：field-analysis.json；采样帧：frames.jsonl；收尾：live-summary.json。
- 本轮助手只读取已有证据、执行GET查询并保存分析文件，没有下单、撤单、平仓或重启服务。

## 参数与时间线（UTC）

基准最新成交价2457.52，ETH-USDT-SWAP，每侧sz=0.1张；ctVal=0.1 ETH/张，名义数量0.01 ETH。cross/split，实际仓位接口lever=125；实验脚本未调整杠杆。

| 事件 | 时间 | 证据 |
| --- | --- | --- |
| 两侧请求开始 | 15:14:06.544 | 两份原始请求均在发送前持久化 |
| 空父单接受 | 15:14:06.705 | ordId=1001125142960933；入场2458.52、SL2468.52 |
| 多父单接受 | 15:14:06.707 | ordId=1001125142960934；入场2456.52、SL2446.52 |
| 多父单触发 | 15:16:31 | triggerTime=1788621391秒、uTime=1788621391000毫秒 |
| 新普通单与TPSL创建时间 | 15:16:31 | 普通单1001125142983995、TPSL1001125142983994，cTime同为1788621391000 |
| 普通单成交 | 15:16:32 | fillPx=2456.52、fillSz=0.1张、ts=1788621392000 |
| 空父单撤销接受 | 15:19:07.797 | 本次父ID1001125142960933，之后活动触发单回读无两张入口父ID |

创建时间相同、方向/价格/数量吻合是本次实验的候选对应证据，不等于交易所显式父子外键。订单号码相邻也不作为归属证明。

## 字段逐接口对照

| 对象/接口 | ordId或posId | clOrdId | tag | 明确父/仓位引用 |
| --- | --- | --- | --- | --- |
| 多侧提交请求 | ordId由服务器生成 | EL4305ffecaf2aL | ET4305ffecaf2aL | 不适用 |
| 空侧提交请求 | ordId由服务器生成 | EL4305ffecaf2aS | ET4305ffecaf2aS | 不适用 |
| 多父单提交回执 | 1001125142960934 | 空字符串 | 空字符串 | 无 |
| 空父单提交回执 | 1001125142960933 | 空字符串 | 空字符串 | 无 |
| 多父触发历史 | 1001125142960934 | 字段不存在 | 字段不存在 | 无childOrdId/posId |
| 新普通单GET详情 | 1001125142983995 | 1001125142983995 | 空字符串 | 无parentOrdId/triggerOrdId/posId |
| 同一普通单orders-history | 1001125142983995 | 空字符串 | 空字符串 | 无 |
| fills | 1001125142983995 | 空字符串 | 空字符串 | 无父单或posId |
| split仓位 | posId=1001125142983995 | 字段不存在 | 字段不存在 | 无父触发单或止损ordId |
| 新TPSL活动记录 | 1001125142983994 | 字段不存在 | 字段不存在 | 无父单或posId |

按EL4305ffecaf2aL和EL4305ffecaf2aS查询GET /deepcoin/trade/order，已记录的结果均为code=0、data=[]。这是未查到这些自定义ID，不表示已接受的父单或已成交的普通单不存在。

子普通单ordId与fills.ordId相同，可直接连接普通单和成交。本次split仓位posId也等于该普通单ordId，是本次样本中明确可展示的等值关系；不能把它扩大为父单ordId相同或所有仓位模式的保证。

多父单1001125142960934与普通单1001125142983995是不同ID。详情clOrdId=1001125142983995属于子单自身ID，并非EL4305ffecaf2aL，也不是父单ID；不能用于验证自定义ID继承成功。

## 最新状态和成本

15:21:42Z刷新：仍有long posId=1001125142983995，pos=0.1张，即0.01 ETH，avgPx=2456.52，slTriggerPx=2446.52。TPSL1001125142983994仍可见，side=sell、posSide=long、sz=0.1、slTriggerPrice=2446.52。价格/方向/数量一致，但TPSL缺明确引用字段，所以不能把候选关联说成API直接返回的血缘。

成交tradeId=1000303880772048、billId=10003038807720480。订单详情与该成交均报告fee=0.00491304、feeCcy=USDT。这里只报告入场手续费字段，不将其视为全部实验成本；仓位尚未关闭，退出手续费、实际价差和潜在资金费尚未结算为完整成本。

程序没有平掉该多仓，也没有撤销它的止损。用户若结束实验，需自行核对并处理剩余仓位；助手没有执行交易操作。

## 证据局限

- 触发历史每次最多最新100条，无当前已确认可用的分页契约；frames.jsonl明确记录trigger_history_latest_100_only_no_supported_pagination。
- REST轮询会存在短暂状态间隙；本次父单触发、普通成交和TPSL活动行均已捕获，但不能宣称全部生命周期无遗漏。
- 只对一侧获得真实成交样本；不能得出空侧、止损触发后执行单或多次触发重建的结论。
- 两个自定义字段一起提交被接受且未观察到传播；没有分别进行仅clOrdId/仅tag组的真实订单，不证明单字段组行为完全相同。
