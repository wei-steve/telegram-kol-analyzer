# Post-Migration Cleanup Status

单进程 → 三角色进程 + 持久化队列 迁移完成后的代码与仓库清理。本文件是跨会话唯一的进度真相；
新会话只读本文件，再打开 `current_step_file` 指向的那一份步骤文件，不要读其他步骤文件。

```yaml
project: post-migration-cleanup
plan_index: docs/plans/2026-09-06-post-migration-cleanup/README.md
brain_session_id: local_858790fe-37cd-426c-a0eb-cbf304066815   # 指挥会话，执行会话完成后必须 send_message 到这里
brain_session_title: 自动项目多线程迁移后的代码清理
integration_branch: codex/deepcoin-auto-trading-v1               # 本地部署分支；每步完成后由指挥会话本地合并，不 push
production_modes: "runtime roles web/ingest/worker (systemd x3); message_pipeline_mode=queue; worker_command_mode=queue; message_lock_mode 设置已删除（ingest 固定按 chat_id 加锁）"
current_step: 6
current_step_file: docs/plans/2026-09-06-post-migration-cleanup/step-6-integrate-and-deploy.md
step_status: in_progress          # planned | claimed | in_progress | completed | blocked
claimed_by: local_3d9720ea-9f2e-494d-961e-a2cd43bfb862
last_completed_step: 5
last_completed_commit: ceac4951
```

## 步骤总览

| 步 | 名称 | 风险等级 | 性质 |
|---|---|---|---|
| 1 | 工作区与文档降噪 | L0 | 不改运行代码 |
| 2 | 默认值翻转与死代码/迁移期注释清理 | L1 | 改设置默认值与注释 |
| 3 | 删除 inline / shadow 消息与命令路径 | L2 | 删代码路径，语义不变 |
| 4 | 锁层收敛与补偿循环去重 | L2 | 需用户先确认方案 |
| 5 | 一次性修复模块归档 + 灰度开关清单 | L1 | 移动模块、产出清单 |
| 6 | 集成部署与 L2 观察 | L2 | 由用户/codex 走既有门控部署流程 |

## 执行会话的领取协议

1. 在新建会话中先读 `AGENTS.md`，再读本文件，再只读 `current_step_file`。
2. 确认 `step_status` 为 `planned`。若为 `claimed` / `in_progress`，停止并告知用户。
3. 把 `step_status` 改为 `claimed`，`claimed_by` 填本会话 ID（用 `get_session self` 取），单独提交这一个文件。
4. 开始改代码前把 `step_status` 改为 `in_progress`。
5. 完成后按步骤文件的"完成条件"更新本文件（`completed`、`last_completed_step`、`last_completed_commit`、在下方证据区追加记录），
   把 `current_step` 推进到下一步并填好 `current_step_file`，`step_status` 回到 `planned`，`claimed_by` 置空。
6. 用 `mcp__ccd_session_mgmt__send_message` 把摘要发给 `brain_session_id`。
7. 全程遵守 AGENTS.md：不用 `git add -A`，只暂存明确路径；不 push、不部署、不发多余 Telegram 通知。

## 硬性禁止（所有步骤）

- 不改任何交易语义：识别、策略、仓位归属、执行、交易所写入的"决定什么"不能变，只能变"在哪里跑、怎么组织"。
- 不动主检出目录里的未跟踪文件（那是 codex 的进行中工作）。
- 不动 `~/.codex/worktrees/*`、`/Users/steven/Documents/telegram获取消息-*`、`/private/tmp/*` 这些外部工作树，只允许列出并报告。
- 不 push，不部署，不改生产设置，不重启服务。

## 证据记录

执行会话在此追加，格式：`- step-N (日期, 会话ID): 提交 SHA；做了什么；验证结果；遗留问题`。

- step-4-decision (2026-09-06, 用户在指挥会话 local_858790fe 确认): 锁层采用**方案 A**——删除 `message_lock_mode` 与 `MessageLockProvider`，ingest 内固定用 `KeyedAsyncLockRegistry` 按 chat_id 加锁，删除 web 端 `lock_all()`；补偿循环按 step-4 文件任务 2 去重。
- step-1 (2026-09-06, local_a288ae52-d04b-43c3-afa6-70eb62636341): 分支 `cleanup/step-1-workspace-and-docs`，提交 4f9ca4c5（归档 385 份 docs/plans）+ 3f88aafa（AGENTS.md 修剪 + 新增 docs/ARCHITECTURE.md）。工作树：仓库内 `.worktrees/` 删除 8 个（干净且相对 origin 无领先提交），`git branch -d` 删除 7 个已合并分支，保留 6 个（chen-management-consistency 领先 13 提交；partial-close-protection 领先 1；context-hold-owner-alert 有 9 项改动；entry-candidate-direction-price-geometry / historical-attribution-cleanup / protection-order-side-semantics 各 1 项未跟踪文件）；`semantic-ai-disagreement-review` 不是本仓库工作树而是独立 git 克隆，未处理。外部工作树（~/.codex/worktrees/*、telegram获取消息-*、/private/tmp/*）按规定只列出未动。虚拟环境：`.venv`（77M，Python 3.12.12，唯一装有 console script 的可用环境）与 `.venv313b`（89M，被 README.md 引用）保留；`.venv313`（26M）与 `.venv313a`（12M）除 .gitignore 外无任何引用且 bin/python 已失效，但它们是主检出目录的未跟踪文件，按硬性禁止条款未删，留待用户决定。文档：docs/plans 441 份 → 保留 56 份、归档 385 份到 docs/archive/plans/（保结构）。AGENTS.md 删除 Runtime Serialization Remediation 整节（其状态文件 current_phase: done）；Runtime Incident AI Agent 一节保留（状态文件仍有 next_phase_after_8r_6a: 8R.6B 与 waiting_for_natural_update）。验证：`pytest tests/test_position_authority_boundary_coverage.py -q` → `7 passed in 0.25s`；`pytest --collect-only -q` → 7462（基线 02e68df7 同为 7462，未减少）。遗留：(1) 删除 Runtime Serialization Remediation 一节同时移走了 AGENTS.md 里“Never run `git add -A` in this repository”这条通用禁令，建议后续步骤把它并入 Project Workflow；(2) `.venv313` / `.venv313a` 待用户裁决；(3) `.worktrees/semantic-ai-disagreement-review` 独立克隆（44M）待用户裁决。
- step-2 (2026-09-06, local_97403b85-06f5-4170-8219-9b7922b8c678): 分支 `cleanup/step-2-defaults-and-dead-code`，三个提交 a95f3415（默认值翻转 + 测试）、f3a9b52d（死引用删除）、d0292216（迁移期注释改写）。任务 1：`trading_settings.py` 的 `message_pipeline_mode` 与 `worker_command_mode` 默认值 `inline` → `queue`，`Literal` 取值集合与 `message_lock_mode` 未动；解析走 `raw.get(key, defaults.<field>)`，DB 里已持久化的值照常读回。测试隐式依赖默认 inline 的共 **32 处**改为显式传参并加注释 `# inline path: scheduled for removal in cleanup step 3`（gap_recovery_loop 9、reconcile_live_history 10、telegram_live_listener 10、reconcile 1、live_listener_chat_isolation 1、message_pipeline_mode_exclusivity 1（inline_app）、web_app 2、message_processing_shadow_enqueue 1；断言语义一律未改，两处仅改测试函数名里的 `inline` → `queue` 以匹配新默认）。新增两个默认值测试：字段缺失 → queue（DB 与空 payload 两条路径）、显式 inline → inline。`cli.py` 的 `--runtime-role` 默认值 `all` 未改，只加了 help 说明生产是 web/ingest/worker 三进程。任务 2：删除 `telegram_live_listener.AUTHORITATIVE_GAP_RECOVERY_MAX_AGE`（全仓 grep 零代码引用，仅历史文档提及）；按 ruff F401/F841 删除 20 个未用导入与 3 个死局部变量（两处未用的 `except ... as exc` 别名、`web_app` 里一个结果被丢弃的只读查询 `backup_by_leg_id` 及随之失效的 `PositionBackupStopOrder` 导入）。**刻意保留** 8 项：`cli.py:5857` 的 `import uvicorn` 是带用户提示与退出码的依赖探测；`web_app` 的 `_schedule_authoritative_notification` 被 `tests/test_web_app.py:6281` 按字符串路径 monkeypatch；`entry_revision_exchange_authority` 的 `is_canonical_idle_entry_revision_exchange_authority` 是给 `manual_pending_entry_reconciliation` 的跨行重导出（一度误删，已用 AST 扫描全仓 import 块与属性访问确认修正）；其余 6 个 F841（`entry_protection_ledger_repair.py:1263 returned_order_id_set`、`message_recognition.py:1410 management_note` 与 `:3030 entry_applied`、`raw_ingest.py:118 inserted_current_message`、`strategy_management_batches.py:555 parent_snapshot`、`trigger_backup_stop.py:60 close_size`）位于识别/策略/交易所写入代码，是潜在缺陷信号而非迁移残留，按硬性禁止条款未动——其中 `entry_protection_ledger_repair.py` 那处与 `recovery_live_submit.py:2461` 是同一段代码的两份拷贝，后者在 2486 行实际使用了该变量，前者丢失了这次使用，建议单独排查。任务 3：改写 **7 处**迁移期注释（`message_lock_provider.py` 模块 docstring + `__call__` docstring 共 5 句、`keyed_async_locks.py` 模块 docstring、`telegram_live_listener.py` 两处 docstring、`lifecycle_monitor.py:664` 行内注释），去掉 "used to hold / byte-for-byte / pre-Phase-2 / rollback path Phase 2 / Phase 3 Task 1 compensator" 等表述，改为描述现状与所在角色进程；只动注释与 docstring，逻辑零改动。`message_processing_worker.py` 与 `web_app.py` 经全文扫描无迁移期注释。`system_operator_bot.py`(839/2691)、`runtime_agent_*.py`、`runtime_incident_handoff.py`、`cli.py:3552` 的 Phase 2/3/5/6 属于仍在进行的 Runtime Incident AI Agent 计划，是活文档而非迁移残留，未动；`legacy` 一词按要求未碰。`docs/ARCHITECTURE.md` 第 3 节模式表与"默认值不一致"那段已改为一致，第 6 节同样过时的一句一并改写。验证：全量 `python -m pytest -q` → `7460 passed, 4 skipped, 32 warnings in 442.24s (0:07:22)`，0 failed；收集数 7464（基线 7462，+2 为新增默认值测试）。`ruff check src/telegram_kol_research`：1546 → 1521 条，逐条 diff 比对**零新增告警**。遗留：(1) 上述 6 个 F841 待步骤 3/5 或单独任务处理，`entry_protection_ledger_repair` 与 `recovery_live_submit` 的拷贝分叉值得优先看；(2) 工作树里 `.venv` 是指向主检出目录 `.venv` 的软链接（`test_minimal_server_updater` / `test_server_update_scripts` 共 15 个测试硬编码 `<repo-root>/.venv/bin/python`，无此链接则在任何工作树里都失败，与本步改动无关），已写进主检出目录的 `.git/info/exclude`，合并后可删链接；(3) 本步 `step_status` 未经过 `in_progress` 中间态——该次编辑未提交，被修正提交 2 时的 `git reset --hard` 丢弃，其余领取协议字段正常。
- step-3 (2026-09-06, local_54ee9d81-eec6-4363-989a-d5de14b3c034): 分支 `cleanup/step-3-delete-inline-shadow`，五个提交 6559dde8（领取）、b350c555（盘点）、71d70c06（in_progress）、38ad65c1（删路径）、a9a5bb90（测试整理，最终 SHA）。相对起点 200e2bf7：`22 files changed, 1055 insertions(+), 2700 deletions(-)`（src 5 文件 +116/−649，tests 14 文件 +753/−2033）。**盘点**：`docs/plans/2026-09-06-post-migration-cleanup/step-3-inventory.md`，逐条标注删/保留并简化/不动，并写下贯穿全文的判定规则：只删 queue 下永不可达的分支体，**不改任何公共函数签名**（`run_reconcile_once` / `recover_missing_authoritative_decisions` / `run_authoritative_gap_recovery_loop` / `run_live_listener` / `persist_live_message_event` 的形参一律保留，留给步骤 4），只删除仅被已删分支引用的模块内私有辅助。**任务 2**：`trading_settings.py` 两个字段 `Literal` 收窄为 `Literal["queue"]`，新增 `_runtime_queue_mode` 解析器——历史值 `inline`/`shadow` 记 `logger.warning` 后返回 `queue`，绝不抛错；其他非法值仍 `ValueError`（模块新增 `logging` 与 module logger）。`telegram_live_listener.py` 删 619 行：`_enqueue_shadow_processing_jobs` → `_enqueue_processing_jobs`（去掉 mode 分派与 `pipeline_mode_override`，插入恒 `shadow=False`），`_try_enqueue_shadow_processing_jobs` → `_try_enqueue_processing_jobs`，`_mark_shadow_processing_jobs_terminal` 与 `_try_mark_shadow_processing_jobs_terminal` 整体删除（只 UPDATE `shadow.is_(True)` 的行，queue 下恒空操作），`_persist_live_message_event_inline` 的 `run_post_persist_processing` 开关与 `process_message_job` 调用块删除、`shadow_enqueue_hook` → `enqueue_hook`，`persist_live_message_event` 包装只剩落库+入队（`last_reason` 恒 `queue_enqueued`），`recover_missing_authoritative_decisions` 只剩"选候选 + 入队 + return"（过期分类与记录已由 worker `_classify_claim_expiry` 承担），`run_reconcile_once` 删掉历史 inline 识别块、trade-idea 合并块、策略提醒块与 shadow 终态标记（`recognition_status` 恒 `"queued"`，`elif authoritative_processor is None` 的错误日志保留为独立 `if`，queue 下行为不变），`_load_reconcile_pipeline_mode` → `_repair_reconcile_history_checkpoints`（保留 `repair_history_checkpoints` 副作用，`tests/test_reconcile.py` 按名 monkeypatch 它），删除只服务该块的 `_load_authoritative_reconcile_projection` / `_count_signal_candidates` 与 4 个失效导入。`message_processing_worker.py` 删 `!= "queue"` 检查，worker 无条件消费（`shadow = 0` 认领过滤**不动**，它防的是历史行）。`web_app.py`：删 `_prepare_web_worker_command` 里恒不可达的 `worker_command_mode_invalid` 守卫及其 `load_trading_settings` 读取（web_app 里**本来就没有** worker command 的 inline 直跑或 shadow 分支，更早阶段已清干净）、删 worker 启动前的 `mode != "queue"` 判断、`/api/trading-settings` 两处 `require_worker_command_mode_transition_safe` 调用、把 parity 端点的 `observed_shadow` 定死为 `False`（响应键名与形状全部保留）。`worker_command_executor.py` 删两处 mode 检查、删 `require_worker_command_mode_transition_safe` 与 `WorkerCommandModeTransitionError`，`supervise_worker_command_mode` 退化为直接委派 `queue_runner`（保留函数名与签名，它是 `app.state.worker_command_worker_runner` 默认值）。`docs/ARCHITECTURE.md` 第 3、4、6 节同步改写。**表结构零改动**。**任务 3**：步骤 2 标记的 36 处注释全部处理完，仓库内该注释归零；测试函数按名统计 **删 45、留 378、新增 22**（收集数 7464 → 7429，净 −35）。两个文件改名以匹配现状：`test_message_pipeline_mode_exclusivity.py` → `test_message_pipeline_queue_path.py`、`test_message_processing_shadow_enqueue.py` → `test_message_processing_enqueue.py`、`test_worker_command_mode_exclusivity.py` → `test_worker_command_supervisor.py`。**覆盖迁移而非丢失**：过期分类（stale/stalled）与它写的审计行从 listener 已删分支搬到 worker tick（新增 3 个用例）；权威失败通知与指令摘要 5+1 个用例从 `persist_live_message_event` 改走 `process_message_job`（同一条链、同一批参数，新增 `_persist_then_process` 辅助）；入队幂等、崩溃后重领、gap recovery 只入队不执行、worker 消费一次且只一次全部保留并改写；worker loop 测试改用 `task.cancel()` 停循环。新增历史值 `inline`/`shadow` → `queue` 且不抛错的用例。**验证**：全量 `python -m pytest -q` → `7425 passed, 4 skipped, 32 warnings in 443.70s (0:07:23)`，0 failed；`tests/test_runtime_event_loop_blocking_census.py` 3 passed；`python -c "import telegram_kol_research.web_app, telegram_kol_research.cli"` 通过；本地 `TELEGRAM_KOL_RUNTIME_ROLE=all` 启动 web（不连 Telegram/交易所，仅有预期的缺凭证与缺表告警）`GET /api/trading-settings` → 200，`message_pipeline_mode=queue`、`worker_command_mode=queue`，消息 worker 任务已起，退出干净。**遗留（盘点文件"留给以后"一节有完整清单）**：(1) `message_processing_jobs.shadow` 列仍在表上（`models.py:123`，`default=True`/`server_default '1'`），本步只保证新行恒 `False`，删列或改默认值是 L3；(2) 删分支后变成未使用的形参一律保留，交给步骤 4 的补偿循环去重一起收（`recover_missing_authoritative_decisions` 8 个、`run_reconcile_once` 8 个、`run_authoritative_gap_recovery_loop` 透传的一组、`run_live_listener`/`_persist_live_message_event_inline` 9 个，外加本步之前就已是死形参的 `lifecycle_monitor` 与 `auto_trade_executor`）；(3) `authoritative_processor` **刻意保留在 listener 签名里**——删掉 `process_message_job` 后它在 ingest 路径仍被引用，作为 reply 目标补齐那段的"识别权威已配置"门（`_persist_live_message_event_inline` 里 `if authoritative_processor is not None and telegram_client is not None and ...`），移除会让这道门恒真属于改语义，生产里它恒非 None（web_app.py:5827）所以保留零成本；(4) **stall 过期聚合通知在代码上彻底没有调用者了**——`StallExpiryNotificationRateLimiter`、`_STALL_EXPIRY_NOTIFICATION_RATE_LIMITER`、`DEFAULT_STALL_EXPIRY_NOTIFICATION_MIN_INTERVAL_SECONDS`、`system_operator_bot.send_stall_induced_expiry_notification` 均保留未删，但 queue 模式下这段本来就不可达，本步只是让它在代码上变得显然；"因系统停顿而过期"现在由 worker 记录审计行，**但没有任何地方再发这条聚合通知**，是否需要在 worker 侧补回值得单独评估；(5) 步骤 2 遗留的 6 个 F841 本步未碰。
- step-4 (2026-09-06, local_a3376d87-d43d-4cc5-b716-3d0b70811b53): 分支 `cleanup/step-4-lock-and-reconcile`，四个提交 778926cc（领取）、0e5f81ea（in_progress）、69dec617（代码）、b0e7ed60（文档，最终 SHA）。相对起点 034d8e76：`20 files changed, 797 insertions(+), 1318 deletions(-)`（src 6 文件 +225/−578）。**任务 1（方案 A）锁层**：`trading_settings.py` 删掉 `message_lock_mode` 字段、`_message_lock_mode` 校验器、`message_lock_expected_mode` 切换守卫；`transition_message_concurrency_settings` 只剩 `message_processing_max_parallel_chats` 的 `BEGIN IMMEDIATE` + 期望值比较交换，新增 `_RETIRED_MESSAGE_LOCK_KEYS`，payload 里带着这两个历史键**不报错**只丢弃，解析器对 DB 里的历史键按未知键静默忽略（新增 12 个用例覆盖）。删除 `message_lock_provider.py`（119 行，`MessageLockProvider` / `resolve_message_lock_mode` / `resolve_lock_context`）；`keyed_async_locks.py` 逻辑一字未改，只改模块 docstring。`telegram_live_listener.py`：`operation_lock` 改为 `KeyedAsyncLockRegistry | None`，两个直播 handler（新消息、删除消息）改为 `registry.lock(chat_id)`；`run_periodic_reconcile` **整段不再持任何锁**（原来 global 模式下整趟 Telegram 拉取都持全局锁），并删掉每轮 `asyncio.to_thread(resolve_message_lock_mode, ...)` 这次读设置；`run_reconcile_once` 新增模块内 `_chat_write_lock` 辅助，**只**把每个会话的「落库 + 入队」那一小段包进该会话自己的锁——这是 reconcile 与直播唯一同时写 `raw_messages` 的地方，而 `raw_messages` 在 `models.py:49` 上**没有** `(chat_id, message_id)` 唯一约束，这把锁是防重复行的唯一手段；worker 的 gap recovery loop 彻底不取锁（它只幂等插 `message_processing_jobs`，且跑在既没有 Telegram 会话也没有这个 registry 的进程里）。`web_app.py` 删除 `telegram_operation_lock` 与 `message_lock_provider` 的构造，两处 `lock_all()` 调用全部删除；`/api/runtime/loop-health` 的锁快照**本来就已经是** `message_lock_registry.snapshot()`，无需改。`cli.py` 的 `alerts` 命令调用点同步收窄。`scripts/per_chat_phase7_observer.py` 按键名 `settings["message_lock_mode"]` 直读该设置，会 KeyError，已删掉它的 `lock_mode` 维度与 `--target-lock-mode` 参数（该脚本是 per_chat 灰度验收工具，灰度不会发生了，是否整体归档留给步骤 5）。**任务 2 补偿循环去重**：`run_reconcile_once` 不再调用 `recover_missing_authoritative_decisions`（该职责由 worker 的 20s `run_authoritative_gap_recovery_loop` 完整承担），返回体去掉只服务这次调用的 `recovered_messages` / `expired_recovery_messages` 两个计数（全仓无 UI/脚本消费者）；`authoritative_gap_recovery_max_age_minutes` 的 stall/stale 过期分类在 worker `_classify_claim_expiry`，一字未动。**顺手收掉 53 个死形参**（`_persist_live_message_event_inline` 11、`run_live_listener` 7、`launch_live_listener_task` 7、`recover_missing_authoritative_decisions` 8、`run_reconcile_once` 8、`run_periodic_reconcile` 5、`run_authoritative_gap_recovery_loop` 7），每个都先 grep 了 src/scripts/tests 全部调用点；`authoritative_processor` 按要求保留（仍是「识别权威已配置」的门）；`StallExpiryNotificationRateLimiter` / `send_stall_induced_expiry_notification` / `position_authority_lock.py` 一律未动（listener 里那条已无用的 import 删了，函数本体在 `system_operator_bot.py` 原样保留）。**web 端互斥点变化**：(a) `/api/trading-settings` 的 `lock_all()` — **判定为本就无效**，它锁的是 ingest 进程，而 `message_processing_max_parallel_chats` 与 `mimo_contract_mode` 都由 worker 进程读取，锁住 ingest 什么也没静默；替代 = DB 层 `BEGIN IMMEDIATE` + 期望值比较交换（原样保留）。(b) `/api/refresh` 的 `lock_all()` — 这一处**在 ingest 进程内是真实有效的**（web 角色会把 refresh 代理给 ingest），删掉会丢两个保证：refresh↔refresh 不重叠、refresh↔直播/周期 reconcile 互斥。前者用新增的 `app.state.refresh_single_flight_lock`（一把只服务 `/api/refresh` 的专用锁，直播路径永不触碰，不会延迟任何一条消息）**原样保留**，`tests/test_web_chat_api.py::test_refresh_api_serializes_overlapping_reconcile_requests` 未改仍通过。后者（refresh 与周期 reconcile / 直播的整趟互斥）**确实失去了**，替代 = 上面那把按 chat_id 的写入锁：唯一会导致状态损坏的 `raw_messages` 重复插入仍被串行化，失去的只是「同一批 Telegram 拉取可能被做两遍」这种浪费。**验证**：`tests/test_live_listener_chat_isolation.py` 整体重写为 5 个用例，覆盖要求的三条保证（同群按到达顺序串行、跨群并行、reconcile 与 live 不互相阻塞）外加删除事件只取自己群的锁、以及 reconcile 写入切片仍与同群直播串行；`tests/test_runtime_event_loop_blocking_census.py` 3 passed；最终候选 b0e7ed60 全量 `python -m pytest -q` → `7423 passed, 4 skipped, 32 warnings in 437.98s (0:07:17)`，**0 failed**；本地 `TELEGRAM_KOL_RUNTIME_ROLE=ingest` 与 `=worker` 分别起 web（不连 Telegram、不连交易所）均 200 且退出干净，ingest 的 `/api/runtime/loop-health` 输出 registry 四个 admission 字段、worker 不输出，两者 `/api/trading-settings` 都是 `pipeline=queue worker_cmd=queue`、响应里已无 `message_lock_mode`。**遗留**：(1) 本步只有两个代码/文档提交而不是步骤文件写的三个——任务 1 与任务 2 落在同一批函数签名上（`run_reconcile_once`、`run_authoritative_gap_recovery_loop` 同时失去锁形参和恢复调用），任何拆法都会留下一个测试不绿的中间提交，故合并为一个代码提交并在提交信息里分段说明；(2) `tests/test_web_app.py::test_message_pipeline_parity_reports_bounded_missing_orphan_and_stuck_jobs` 原本就是竞态用例（`runtime_role="all"` 下 worker 会把它埋的 pending 作业认领走），本步删掉两个循环里每轮的 `to_thread` 读设置后更容易触发，已改用 `runtime_role="web"`（不起任何单例任务）使其确定化，断言一字未改；(3) `scripts/per_chat_phase7_observer.py` 与 `tests/test_per_chat_phase7_observer.py` 是 per_chat 灰度验收工具，本步只做了最小适配让它不再 KeyError，整体是否归档建议并入步骤 5；(4) 步骤 2/3 遗留的 6 个 F841、`message_processing_jobs.shadow` 列、stall 过期聚合通知无调用者这三项本步未碰。
- step-5 (2026-09-06, local_72d9bbf9-0112-47ef-980d-610ffab2e8ef): 分支 `cleanup/step-5-one-off-and-flags`，六个提交 6d29605b（领取）、4af03d8d（in_progress）、d614e936（模块盘点）、407414c5（移动 + 守护测试）、aecbfc30（开关清单）、ceac4951（追加任务 A/B + ARCHITECTURE，最终 SHA）。**任务 1 盘点**：`docs/plans/2026-09-06-post-migration-cleanup/step-5-inventory.md`。用 AST 建全包 import 图（含函数体内延迟 import），从 `web_app.py`（它自身 import 了 `RUNTIME_ROLE_SINGLETON_TASKS` 全部 loop 函数）、`message_processing_worker.py`、`strategy_management_worker.py` 三个根做传递闭包，243 个 `.py` 里 **188 个在线**。47 个候选模块判定为 **keep-online 39 / move-one-off 1 / unsure 7**。判「已完成」时实测出三条硬区分：出现在 `docs/runbook.md` 且带 dry-run→apply 步骤的是**常备运维工具**（`backfill`/`backup_stop_repair`/`evidence_backfill`/`management_history_recovery`/`position_attribution_repair`/`protection_incident_convergence`/`tpsl_ledger_backfill`/`position_management_liveness_recovery` 等）；只出现在 `docs/archive/plans/**` 的 `Create:`/`Test:`/`RED→GREEN` 行里的是**建造记录**不是执行记录；`docs/per-chat-durable-lanes-status.md` 里 `*_production_apply_plan_status: not_built_not_authorized` 的是**造好但从未在生产 apply**（`batch150_management_terminalization`、trigger_protection 那组），属在建工作必须保留。**与步骤文件的两处出入**（均按「宁可少动」处理，未移动）：(a) `position_management_liveness_recovery` 与 `worker_command_reconciliation` 静态上只被 `cli.py` import、不在在线闭包内，与「已知在线」的表述不符——两者都无完成记录，按步骤文件显式指示记 `keep-online`；(b) `entry_assembly_fingerprint_repair` 被步骤文件列进「很可能一次性」，实测被 `production_safety_monitor` import，**确实在线**，已改判 `keep-online`。**任务 2 移动**：新建 `src/telegram_kol_research/one_off/`（带中文 docstring 说明「已执行完毕、仅为可追溯、不得被在线路径 import」），只 `git mv` 了 **1 个**模块 `historical_management_terminalization.py`（1607 行）及其测试到 `tests/one_off/`。它同时满足三条：生产零引用（只有测试引用，自带 `__main__`，全仓无 `-m` 调用文档）；`docs/archive/plans/2026-08-21-historical-management-terminalization-l3-v2.md` §11.3 明写该演练计划「is now evidence only and must not be used for production apply」、其 CAS 门已 fail closed；§14 记录所有者已验收目标终态（batches `123,127,129,133,144,146` resolved、legs `496,497,503,511,530,531,540` closed、bindings `282,283,287,292,307,313` closed 且 `pos_id=NULL`、lifecycles `816,819,834,859,910,921` exited）。`cli.py` **一字未改**（它本来就不 import 该模块）。新增守护测试 `tests/test_one_off_isolation.py`（2 个用例，带 `architecture` 标记）：AST 静态扫描 `src/telegram_kol_research/**`，`one_off` 之外的模块不得 import `telegram_kol_research.one_off.*`，`cli.py` 除外；第二个用例防守护变空（子包必须存在且非空）。已用绝对与相对两种 import 形式各做一次反证，守护均正确失败后还原。**任务 3 开关清单**：`docs/plans/2026-09-06-post-migration-cleanup/step-5-flag-inventory.md`，14 个 `Literal` + 4 个 `*_enabled` 共 **18 个**开关，建议 **collapse-to-live 2 / keep 5 / ask-owner 11**。`collapse-to-live` 是 `message_pipeline_mode` 与 `worker_command_mode`（步骤 3 已把 `Literal` 收窄为单值，剩下的是字段本身与历史值兼容解析器，删字段会动到 DB 历史键与 `/api/trading-settings` 响应键，不是纯内部清理）。11 个 `ask-owner` 里有 **6 个生产值是 `unknown`**——本仓库的定期生产快照只固定采样 `auto_trade_enabled` 与 `worker_command_mode`，其余开关只能靠零散记录，按规则一律写 `unknown` 未猜。能确证的生产值：`auto_trade_enabled=true`（2026-09-05 复核）、`management_execution_mode=live`（monitor unit ExecStart 硬编码 `--expected-management-mode live`）、`composite_management_v2_mode=live`（双向 readback）、`trigger_protection_stop_rescue_mode=live`（≤2026-08-03）、`position_management_liveness_v2_mode=live`（≤2026-09-04）、`trigger_protection_lineage_attribution_mode=disabled`（2026-09-04 激活记录明写未改设置）、`deepcoin_contract_specs_mode=live`（≤2026-08-27）、`mimo_contract_mode=v1`、`context_resolution_enabled=true`（白名单 33 个 chat）。**本步未改任何开关的逻辑或值。** **追加任务 A**：`scripts/per_chat_phase7_observer.py` → `scripts/archive/`、`tests/test_per_chat_phase7_observer.py` → `tests/archive/`（并把测试里的 `parents[1]/scripts/...` 改成 `parents[2]/scripts/archive/...`）；`pyproject.toml` 新增 `norecursedirs`，**同时把 pytest 的 9 条默认模式逐字写回**（设这个键会覆盖默认值，只写 `tests/archive` 会让 `.*`、`build`、`venv` 等默认排除失效）。**追加任务 B**：`README.md:26` 的 `.venv313b` → `.venv`，只改这一行。`docs/ARCHITECTURE.md` 第 5 节整节替换为核实后的分类（在线主路径 / 名字像一次性但在线的 24 个 / 仅 CLI 可达的常备运维工具 / `one_off/` 已归档 / 造好未 apply 的 unsure 11 个），并纠正原文「`reconcile.py` 是 ingest 单例任务」——那个任务其实是 `telegram_live_listener.run_periodic_reconcile`，模块 `reconcile.py` 只有纯函数 `build_reconcile_window`、生产零引用。**验证**：最终候选 ceac4951 全量 `python -m pytest -q` → `7357 passed, 4 skipped, 32 warnings in 447.26s (0:07:27)`，**0 failed**；收集数 7429 → 7361（−68 为 tests/archive 不再收集，+2 为新增守护测试，净 −66，与 passed 数差值一致）。`grep -c "\.command(" cli.py` = **52**，与本步前完全相同（`cli.py` 未改）；`python -m telegram_kol_research.cli --help` 列出 51 个顶层条目，与移动前逐行 diff **完全一致**（52 与 51 的差是 `deepcoin-contract-specs` 是带 `status`/`refresh` 两个子命令的 sub-typer，属既有事实，非本步造成）；守护测试 2 passed。**遗留**：(1) 7 个 `unsure` 中 `frozen_exchange_empty_state_alignment.py`（958 行）在全仓文档里零记录、来历不明，建议单独问所有者；(2) `reconcile.py` 是确定的孤儿（生产零引用），但不在本步「只移动一次性修复模块」的授权范围内，未动；(3) 开关表第三列的 6 个 `unknown` 建议下一轮先补一次完整的 `GET /api/trading-settings` 只读快照并写进 `docs/`，否则任何「删 disabled/shadow 分支」都没有依据；(4) 收敛开关有依赖链 `auto_trade_enabled` → `management_execution_mode` → {composite / stop_rescue / liveness_v2} → `lineage_attribution`，上游 `effective_*` 规则被下游复用，必须自上而下一次一个；(5) 步骤 2/3/4 遗留的 6 个 F841、`message_processing_jobs.shadow` 列、stall 过期聚合通知无调用者三项本步未碰。
