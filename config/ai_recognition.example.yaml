mode: ai_provider
text_provider:
  base_url: "https://api.deepseek.com"
  api_key: "your_deepseek_api_key"
  model: "deepseek-v4-flash"
  timeout_seconds: 60
image_provider:
  base_url: "https://open.bigmodel.cn/api/paas/v4"
  api_key: "your_zhipu_api_key"
  model: "glm-ocr"
  timeout_seconds: 60
recognition_prompt: |
  你是 Telegram 加密货币 KOL 消息的交易策略识别器。
  判断单条消息是否包含可执行的新开仓策略。

  只有同时满足这些条件，才判定为“是策略”：
  1. 有明确交易标的，例如 BTC、ETH、SOL、DOGE、BNB。
  2. 有明确方向：多、做多、开多、long，或空、做空、开空、short。
  3. 有明确入场方式：价格、区间、市价、到价进入、挂单区间。
  4. 至少有止损、止盈、无效价、保护价或分批止盈计划之一。
  5. 表达的是新开仓/新挂单/可执行入场，不是已有仓位管理。

  图片消息：请直接读取图片中的文字和图表标注，不要依赖外部 OCR。

  只输出 JSON，不要输出解释文字：
  {
    "recognition_result": "是策略 | 非策略 | 识别失败",
    "reason": "一句话说明原因",
    "strategy": {
      "symbol": null,
      "side": null,
      "entry": null,
      "stop_loss": null,
      "take_profit": null,
      "leverage": null,
      "order_type": null
    },
    "confidence": 0.0
  }

lifecycle_event_prompt: |
  你是 Telegram 加密货币 KOL 策略生命周期事件判定器。
  你会收到：当前消息、同群最近的活跃策略列表、以及最近聊天上下文。

  你的任务不是识别新策略，而是判断“当前消息”是否在改变某一条已有策略的状态。

  只允许输出 JSON，不要输出解释文本：
  {
    "event_type": "none | entry_confirm | cancel_entry | exit_position | position_update",
    "target_lifecycle_id": null,
    "symbol": null,
    "side": null,
    "entry_price": null,
    "exit_price": null,
    "management_action": null,
    "confidence": 0.0,
    "reason": "一句话说明判断依据"
  }

  判定规则：
  - entry_confirm：当前消息是在通知之前 pending_entry 策略现在/现价/市价/直接入场，或明确说已经进场。
  - cancel_entry：当前消息是在取消之前 pending_entry 限价挂单或等待入场策略，例如取消限价、撤单、取消挂单、等后续信号。
  - exit_position：当前消息是在关闭已 entered 策略，例如平仓、全平、离场、临时离场、止盈了、止损了、先出来、保本出局、成本附近保本出局、保本走、成本走、breakeven exit。
  - position_update：当前消息是在管理已 entered 策略但没有完全离场，例如提前止盈一半、止盈一半、减仓一半、带保护、保护止损、上移止损、推保护、继续持有。management_action 可输出 partial_take_profit、move_stop_to_protect、hold_update、risk_update。
  - none：普通聊天、行情观点、广告、复盘、联系方式、无法确定目标策略、或只是识别新策略但不改变已有策略。
  - 必须优先依据当前消息，不要把上下文里的旧消息当成当前动作。
  - 如果能明确对应活跃策略，请输出 target_lifecycle_id。
  - 如果不能唯一对应，event_type 必须为 none 或 confidence 低于 0.7。
  - confidence 低于 0.7 时，系统不会执行状态变更。
